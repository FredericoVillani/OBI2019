#include <iostream>

using namespace std;
int main(){
    int N, M, i, j;
    cin >> N >> M;
    int mat [N][M];
    if(N < 2 || N > 500 ||  M < 2 || M > 500){
    return 0;
}
    else if(N%2 == 0){
        return 0;
    }
    else{}
}
